---
version: 1
slug: "web-src-pages-marketing-landingpage-tsx"
primary_target: "web/src/pages/marketing/LandingPage.tsx"
related_targets: []
---

## Direction contract

THESIS: the page's own scroll is the mechanism, not a description of it — a single SVG coil visibly tightens as you pass each real stage of the Ouroboros loop, making "research that feeds itself" something felt, not read.

OWN-WORLD: unchanged locked identity — `#15120F` ground, `#FB631B` ember-orange (the coil's only ink), Fraunces display / IBM Plex Sans body / IBM Plex Mono for real numbers only; flat surfaces, hairline dividers, no shadows, no gradients.

STORY: a visitor (a judge, a studio evaluator, a curious engineer) lands not knowing what Ouroboros is; by the end of one scroll they've seen the actual loop (ingest → triage → verify → monitor → re-verify → repeat) enacted stage by stage, seen one real number (live Reality Drift from the deployed API), and know exactly where to go next — the dashboard or the docs.

FIRST VIEWPORT: full-height hero — wordmark + "Research that feeds itself" + one-line positioning, with a large, loose single-turn coil (SVG path, ember-orange stroke) already visible, hinting it will tighten as they scroll. No CTA button competing for attention yet — the coil is the promise.

FORM: dealt structure "The Coil, Ascending" (index 3 of 7 grounded candidates), raised by two fused challengers — clay-ceramics-coiled-earth-tower's ascending-courses topology (each loop stage is a stacked "course" as you scroll, one course lit in the accent color at a time) and aww2-zoo-gardens-guide-map's single-geometry technique (one continuous SVG path drives the coil illustration, the active-stage highlight, and section transitions together, not three separate assets). Seed key 4deed216.

FINISH: unreviewed and undocumented is unfinished; this build ends with the finish review, the verdict, DESIGN.md, and every shipping raster carrying its provenance.
