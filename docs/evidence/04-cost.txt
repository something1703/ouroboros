Phase 4 — cost metering evidence
===================================

packages/parallel_client/smoke.py (PHASE_04.md §4.1 acceptance: "performs one turbo
search and prints cost; cost_events row exists"), run against local docker-compose
Postgres:

$ uv run --env-file .env python -m packages.parallel_client.smoke demo
HTTP Request: POST https://api.parallel.ai/v1/search "HTTP/1.1 200 OK"
{"api": "search", "sku": "search.turbo", "claim_id": null, "latency_ms": 659, ...}
search_id=search_5b24bdc21097adf00c3bc44e345c93fb hits=2
  'Fireside Chat with Parag Agrawal, Founder & CEO of Parallel Web Systems' — https://parallel.ai/press
  'Former Twitter CEO Parag Agrawal's Parallel Web Systems has raised $100 million: WSJ ...' — ...
project 'demo' total spend: $0.0010 (cost_events row written)

scripts/verify_claim.py's CostMeter integration (search + task, both wrapped per
PARALLEL_INTEGRATION.md §2's "meter before, correct after" contract), confirmed against
the real Cloud SQL "demo" project after the two real runs in docs/evidence/04-verify.txt
(2x search.fast @ $0.001, 1x task.core-fast @ $0.025 — the Coca-Cola run's
overall_confidence came back "high", so should_escalate correctly did not fire a
task.pro call despite --escalate being passed):

$ uv run python scripts/_check_spend_tmp.py   # temporary helper, run via Cloud Run Job
demo project total Parallel spend so far: $0.0260

Total spend across all of Phase 4 development (client-core smoke test, wrapper live
tests in tests/packages/parallel_client/test_live.py, the two verify_claim.py runs
above, and ad-hoc manual verification of every wrapper during implementation) stayed
well under the $2 Parallel exit-gate cap — the single largest line items were the ~6
real Task calls made across this phase (core-fast @ $0.025 each).
