# fixtures

Sample screenplay PDFs, rough cuts, project metadata, and recorded Parallel API cassettes. Large media (PDFs, video) are gitignored and fetched from GCS — see `docs/DEV.md` once Phase 1.4 lands.
