# packages/claims

Pydantic v2 domain models (`Claim`, `Evidence`, `Risk`, ...), text normalization, `claim_id` hashing, deterministic risk pre-scoring, and Reality Drift. See `DATA_MODEL.md` §1, §6.
