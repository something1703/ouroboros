"""PHASE_08.md §8.5: server-side export generation (E&O evidence pack, clearance-log
Sheet, fact-check report). Its own package, not folded into `packages/claims` or
`packages/ledger`, because it's presentation logic over those models, not domain logic."""
