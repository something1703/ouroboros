# packages/parallel_client

The only code in Ouroboros allowed to import `parallel_web`. Search, Task, Responses, Entity Search, Extract, Monitor, Memory wrappers, cost metering, and webhook signature verification. Contract: `PARALLEL_INTEGRATION.md`.
